# configuration file for production environment

# only log warning and error messsages
log: "warning"

# log message to a file in logs/
logger: "file"

# don't consider warnings critical
warnings: 0

# hide errors 
show_errors: 0

log_path: "/var/log/showkind"

# cache route resolution for maximum performance
route_cache: 1

plugins:
  Database:
    driver: 'mysql'
    database: 'showkind'
    host: 'localhost'
    port: '3306'
    username: 'showkind_work'
    password: 'Ahzoob4g'
